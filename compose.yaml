services:
  backend:
    image: snakebyte.azurecr.io/ohmyword-api
    build:
      context: BackEnd
      dockerfile: Dockerfile
    environment:
      - AZURE_TENANT_ID=${AZURE_TENANT_ID}
      - AZURE_CLIENT_ID=${AZURE_CLIENT_ID}
      - AZURE_CLIENT_SECRET=${AZURE_CLIENT_SECRET}
